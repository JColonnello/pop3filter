#ifndef MGMT_H
#define MGMT_H

typedef enum
{
	ERROR,
	STATS,
	SET_ERROR_FILE,
	GET_ERROR_FILE,
	SET_PROXY_ADDR,
	GET_PROXY_ADDR,
	SET_LISTEN_PORT,
	GET_LISTEN_PORT,
	SET_ORIGIN_PORT,
	GET_ORIGIN_PORT,
	SET_FILTER,
	SET_MGMT_ADDR,
	GET_MGMT_ADDR,
	SET_MGMT_PORT,
	GET_MGMT_PORT
} RequestStatus;

#endif
